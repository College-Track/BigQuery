  --Qs for BR/IR
    --Can someone speak to the WITH ... AS intro / syntax vs. starting with SELECT or doing SELECT and using AS after the SELECT?
    
    SELECT
    -- Select fields from templates
    x_18_digit_id_c,
    Full_Name_c,
    site_text_c,
    current_cc_advisor_2_c,
    high_school_graduating_class_c
    
    FROM
    `data-warehouse-289815.salesforce_clean.contact_template` AS student_list
    WHERE College_Track_Status_c IN ('11A', '12A','15A', '16A');
    
    SELECT
    -- Select fields from templates
    id AS bb_app_id,
    student_c AS student_id,
    finance_bb_balance_total_c,
    
    FROM
    `data-warehouse-289815.salesforce_clean.scholarship_application_clean`
    LEFT JOIN "student_list"  ON student_id = x_18_digit_id_c
    WHERE
    record_type_id = "01246000000ZNi1AAG"
    AND finance_bb_balance_total_c > 0