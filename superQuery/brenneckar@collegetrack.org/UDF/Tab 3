SELECT
  Contact_Id,
  GPA_Semester_c,
  `data-warehouse-289815.UDF.determine_buckets`(GPA_Semester_c,.5, 2.5, 3.5, "") as gpa_bucket,
  attendance_rate_c,
  `data-warehouse-289815.UDF.determine_buckets`(attendance_rate_c, 10, 65, 90, "%") as attendance_bucket
FROM
  `data-warehouse-289815.salesforce_clean.contact_at_template`
WHERE
  college_track_status_c = '11A'
  AND gpa_semester_c IS NOT NULL
LIMIT
  1000